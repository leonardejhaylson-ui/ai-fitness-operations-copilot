# Architecture Decision Records

Use sequential names such as `0002-short-title.md`. Each ADR contains: title, date, status, context, decision, consequences, and links to affected normative sources. Status starts as `Proposed` and becomes `Accepted` only through review. ADRs clarify implementation choices; they cannot silently override approved artifacts.
